---
const { title, copy, button, imageUrl } = Astro.props;
---

<div class="relative flex flex-col items-center p-4 w-full text-center text-white bg-card-bg shadow-card overflow-hidden transition-transform duration-700 ease-out">
  <div class="absolute inset-0 bg-cover bg-center" style={`background-image: url(${imageUrl});`} />
  <div class="relative z-10 flex flex-col items-center p-4">
    <h2 class="text-2xl font-bold mb-2">{title}</h2>
    <p class="font-serif text-lg italic mb-4">{copy}</p>
    <a href="#" class="px-4 py-2 text-sm font-bold uppercase text-white bg-btn-bg border-none hover:bg-btn-hover-bg focus:outline-yellow-400 focus:outline-1 focus:outline-offset-2">
      {button}
    </a>
  </div>
</div>
